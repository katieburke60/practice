//When doing an API call you typically want  three actions:
  //1. Tell reducer that you're fetching data from the API
  //2. Tell reducer that request finished successfully - can merge data into its state.
  //3. Tell reducer that request failed (can log an error)
