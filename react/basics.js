//React elements are immutable. The only way to upate the UI is to create a new element and pass it to ReactDOM render.

//Components are like functions. Accept arguments called 'props' and return React elements describing what should be on a page. 
