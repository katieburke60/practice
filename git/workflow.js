
//1 make a new project directory
//2 initialize a git file (git init)
//3 make and co a new feature branch
//4 make changes add and commit them
//5 Checkout the master branch and merge changes into master from the feature branch (git merge)

//6 push to the new github branch
//7 go to github repo and edit the title, add more information to the description if needed.
//8 click Create pull request
//9 merge pull request to the master.

--
// git checkout -b (this creates a new branch and checks out to that same branch)
// do work
// commit (add, commit)
// git checkout master
// git pull origin master
// git checkout
// git merge master
// fix conflicts! (tells you on command line where to look in editor)
// run tests if applicable
// git push origin
// go to GitHub! (hub browse)
// create a pull request(make sure its going to the right place)
// hit the ‘submit pull request’ button
// contact partner to review pull request
// partner merges pull request
// git checkout master, git pull origin master, and repeat the process
